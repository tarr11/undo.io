<% slide.sections.each_with_index do |section, index| %>
        <% if index == 0%>
        <h1>
        <% else %>
        <h2>
        <% end %>
          <%= render 'task_checkbox', :section => section%>
          <% section.slide_contents.each do |content| %>
              <%= render 'slide_content', :content => content %>
          <% end %>
    <% if index == 1%>
    </h1>
    <% else %>
    </h2>
    <% end %>
<% end%>

