<%= form_for(@team_product_request) do |f| %>
  
  <% if @team_product_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_product_request.errors.count, "error") %> prohibited this team_product_request from being saved:</h2>
      <ul>
      <% @team_product_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :feedback %><br />
    <%= f.text_area :feedback, :rows=>10 %>
  </div>
  <div class="actions">
    <%= f.submit "send" %>
  </div>
<% end %>
