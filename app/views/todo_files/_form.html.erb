<%= form_for(@todo_file) do |f| %>
  <% if @todo_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo_file.errors.count, "error") %> prohibited this todo_file from being saved:</h2>

      <ul>
      <% @todo_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :filename %><br />
      <%= f.text_field :filename %>
    </div>

  <div class="field">
    <%= f.label :contents %><br />

    <%= f.text_area :contents, {:style => "width:1000px;height:500px;"} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
